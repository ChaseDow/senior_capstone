<div class="mx-auto flex min-h-[calc(100vh-4rem)] w-full max-w-6xl items-center px-6 py-12">
  <div class="grid w-full grid-cols-1 gap-10 md:grid-cols-2">
    <div class="space-y-6">
      <div class="inline-flex items-center gap-2 rounded-full border border-zinc-800 bg-zinc-900/40 px-3 py-1 text-xs text-zinc-300">
        <span class="h-2 w-2 rounded-full bg-emerald-400"></span>
        Planner + dashboard foundation
      </div>

      <h1 class="text-4xl font-semibold tracking-tight text-zinc-50 sm:text-5xl">
        Clear
      </h1>

      <p class="text-base leading-relaxed text-zinc-300">
        A clean, fast dashboard for your schedule, events, and life. Sign in to continue or create an account in seconds.
      </p>

      <div class="flex flex-wrap items-center gap-3">
        <% if user_signed_in? %>
          <%= link_to "Go to dashboard",
                authenticated_root_path,
                class: "rounded-xl bg-emerald-500 px-4 py-2 text-sm font-medium text-zinc-950 hover:bg-emerald-400" %>

          <%= button_to "Sign out",
                destroy_user_session_path,
                method: :delete,
                data: { turbo: false },
                class: "rounded-xl border border-zinc-800 bg-zinc-900/40 px-4 py-2 text-sm text-zinc-100 hover:bg-zinc-900" %>
        <% else %>
          <%= link_to "Create account",
                new_user_registration_path,
                data: { turbo: false },
                class: "rounded-xl bg-emerald-500 px-4 py-2 text-sm font-medium text-zinc-950 hover:bg-emerald-400" %>

          <%= link_to "Sign in",
                new_user_session_path,
                data: { turbo: false },
                class: "rounded-xl border border-zinc-800 bg-zinc-900/40 px-4 py-2 text-sm text-zinc-100 hover:bg-zinc-900" %>
        <% end %>
      </div>

      <div class="grid grid-cols-1 gap-3 pt-4 sm:grid-cols-2">
        <div class="rounded-2xl border border-zinc-800 bg-zinc-900/40 p-4">
          <div class="text-sm font-medium text-zinc-100">Simple</div>
          <div class="mt-1 text-sm text-zinc-400">No clutter. Just what you need.</div>
        </div>
        <div class="rounded-2xl border border-zinc-800 bg-zinc-900/40 p-4">
          <div class="text-sm font-medium text-zinc-100">Secure</div>
          <div class="mt-1 text-sm text-zinc-400">Devise auth + reset flow.</div>
        </div>
      </div>
    </div>

    <div class="hidden md:block">
      <div class="rounded-3xl border border-zinc-800 bg-gradient-to-b from-zinc-900/40 to-zinc-950 p-6">
        <div class="rounded-2xl border border-zinc-800 bg-zinc-950/60 p-6">
          <div class="text-sm font-medium text-zinc-100">Preview</div>

          <div class="mt-2 space-y-3">
            <%= image_tag("cia-terry.gif", alt: "Description of the GIF") %>
          </div>
          <div class="mt-4 text-xs text-zinc-500">
            Your real dashboard lives behind login.
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
