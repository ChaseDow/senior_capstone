<% content_for :page_title, "Work Shifts" %>
<% content_for :page_subtitle, "Your work schedule" %>
<div class="rounded-2xl p-6" style="border: 1px solid var(--studs-border); background-color: var(--studs-panel-bg);">
  <div class="mb-4">
    <div class="text-base font-semibold text-zinc-100">New shift</div>
    <div class="text-sm text-zinc-500">Add a work shift to your schedule.</div>
  </div>
  <%= render "form", work_shift: @work_shift, turbo: false %>
</div>