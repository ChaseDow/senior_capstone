<% content_for :page_title, "Syllabuses" %>
<% content_for :page_subtitle, "Current uploaded syllabuses"%>
<div class="rounded-2xl border border-zinc-800 bg-zinc-900/40 p-6">
  <div class="flex items-center justify-between">
    <div>
      <h1 class="text-xl font-semibold text-zinc-100">New Syllabus</h1>
      <p class="mt-1 text-sm text-zinc-400">Upload a new syllabus.</p>
    </div>

    <%= render Studs::SecondaryButtonComponent.new(label: "Back", href: courses_path) %>
  </div>

  <div class="mt-6">
    <%= render "form", course: @course %>
  </div>
</div>