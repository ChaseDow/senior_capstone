<% content_for :page_title, "AI Chat" %>
<% content_for :page_subtitle, "Consult with an AI" %>
<div class="h-full min-h-0 flex flex-col"
     data-controller="full-height"
     style="background-color: var(--studs-panel-bg-2);">
  <!-- Header -->
  <div class="shrink-0 flex items-center justify-between px-6 py-4 backdrop-blur"
       style="border-bottom: 1px solid var(--studs-border); background-color: var(--studs-panel-header);">
    <div>
      <h1 class="text-base font-semibold text-zinc-100">Assistant</h1>
      <p class="text-xs text-zinc-400 mt-0.5">Ask questions, explore scenarios, and compare schedule what-ifs.</p>
    </div>
    <div class="flex items-center gap-2.5">
      <%= button_to "Reset",
        reset_ai_chat_index_path,
        method: :delete,
        class: "inline-flex items-center rounded-full px-3 py-1 text-xs font-medium transition hover:brightness-110",
        style: "border: 1px solid var(--studs-border); background-color: transparent; color: #d4d4d8;" %>
      <span class="inline-flex items-center gap-1.5 rounded-full px-3 py-1 text-xs font-medium"
            style="border: 1px solid rgba(34, 197, 94, 0.3);
                   background-color: rgba(34, 197, 94, 0.1);
                   color: #86efac;">
        <span class="h-1.5 w-1.5 rounded-full"
              style="background-color: #22c55e;
                     box-shadow: 0 0 8px rgba(34, 197, 94, 0.7);"></span>
        Online
      </span>
    </div>
  </div>
  <!-- Flash -->
  <%= render "ai_chat/flash" %>
  <!-- Messages -->
  <div
    id="ai_chat_messages"
    class="flex-1 min-h-0 overflow-y-auto px-6 py-5 flex flex-col justify-start gap-3 scrollbars"
  >
    <% @messages.each do |m| %>
      <%= render "ai_chat/message", m: m %>
    <% end %>
  </div>
  <!-- Message Form -->
  <%= render "ai_chat/message_form", message_cap_reached: @message_cap_reached %>
</div>
