<div class="mx-auto max-w-5xl">
  <div class="rounded-2xl border border-zinc-800 bg-zinc-900/40 p-6">
    <div class="flex items-center justify-between">
      <div>
        <h1 class="text-xl font-semibold text-zinc-100">New Event</h1>
        <p class="mt-1 text-sm text-zinc-400">Create a new event that isn’t tied to classes or work.</p>
      </div>

      <%= render Studs::SecondaryButtonComponent.new(label: "Back", href: events_path) %>
    </div>

    <div class="mt-6">
      <%= render "form", event: @event %>
    </div>
  </div>
</div>
