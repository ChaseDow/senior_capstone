<div class="mx-auto max-w-5xl">
  <div class="rounded-2xl border border-zinc-800 bg-zinc-900/40 p-6">
    <div class="flex items-center justify-between">
      <div>
        <h1 class="text-xl font-semibold text-zinc-100">Edit Event</h1>
        <p class="mt-1 text-sm text-zinc-400">Update event details.</p>
      </div>

      <%= render Studs::SecondaryButtonComponent.new(label: "Back", href: event_path(@event)) %>
    </div>

    <div class="mx-auto max-w-5xl px-6 py-4">
      <%= render "form", event: @event %>
    </div>
  </div>
</div>
